# task-app PROJECT STATUS

## 正本
- ソースコード: GitHubリポジトリ `hiro-saitoh-sr/task-app`
- 本番公開先: GitHub Pages
- 本番データ: Firebase Realtime Database

## Git状態
- 対象ブランチ: `main`
- 直近コミット: 顧問先マスタの「担当者名」を「宛名」に表示名変更・納品メール下書きの「様」自動付与を削除（2026-08-28）
- 2026-08-28作業開始時点で `origin/main` と同期済み（ahead 0 / behind 0）
- 2026-08-28 17:19頃〜18:05頃、GitHub Pagesデプロイ障害が発生（約45分間デプロイできない状態）。原因はPages Source設定の不整合（詳細は「既知の課題」参照）。コミット`d5c7ba4`のpushで正常なデプロイに復旧済み。

## 現在の実装状況
- 案件管理、請求集計、月変チェック、年更算定、給与計算を実装
- 業務監査員Ver.0が `cases` と `masters` を参照
- 業務監査員Ver.0は実行履歴上、安定稼働中
- Firebase Authenticationの許可対象は業務用アカウント2件（Firebase側で管理）
- CodexとClaude Codeは対等な開発担当であり、共通Git手順と競合停止ルールを適用する

## 直近の変更
- 顧問先マスタの詳細表示のCC欄を改行形式（名前行・メール行の2行）に戻す（2026-09-18）。
  - 背景：直前の修正でCC①②③を横並び1行表示にしたところ、「改行形式（名前とメールを別行）に変更してほしい」との指摘があったため差し戻し。
  - 修正内容：`CustomerDetailModal`のCC①②③行を、横並び1行から「名前：〇〇」「メール：〇〇　[コピー]」の2行表示に変更。ただしメールアドレスの折り返しは引き続き防止するため、各行に`whitespace-nowrap`を指定（`break-all`には戻していない）。コピーボタンがメールアドレスのみをコピーする動作は維持。モーダル幅（`min(720px, 95vw)`）は変更なし（改行形式でも折り返し防止のため据え置き）。
  - 影響範囲：`CustomerDetailModal`（詳細表示）のみ。編集モーダル（`MasterItemModal`）・一覧テーブル・データ構造には変更なし。
  - Babel（`@babel/preset-react`相当、esbuildでJSX構文検証）で構文検証済み。Chrome拡張機能が未接続のため実ブラウザでの表示確認は未実施。
- 顧問先マスタの詳細表示のCC欄を横並び1行表示に変更、モーダル幅を拡大（2026-09-18）。
  - 背景：直前の修正でCC①②③を「名前：〇〇」「メール：〇〇」の2行表示にしたところ、「名前・メールを横並びで1行に表示してほしい」との指摘があったため、1行表示に変更。あわせてメールアドレスが折り返されないよう表示幅を拡大。
  - 修正内容：`CustomerDetailModal`のCC①②③行を、名前行・メール行の2行（`<div>`を縦積み）から、「CC①　名前：〇〇　メール：〇〇　[コピー]」の横並び1行（`flex items-center whitespace-nowrap`）に変更。コピーボタンがメールアドレスのみをコピーする動作は維持。
  - モーダル幅：`CustomerDetailModal`の`modal-box`幅を480px→`min(720px, 95vw)`に拡大し、CC行のメールアドレスが折り返されず1行に収まるようにした。他の項目（業種・代表者名等）の表示幅にも余裕ができるが、レイアウト自体（ラベル幅・区切り線等）は変更なし。
  - 影響範囲：`CustomerDetailModal`（詳細表示）のみ。編集モーダル（`MasterItemModal`）・一覧テーブル・データ構造には変更なし。
  - Babel（`@babel/preset-react`相当、esbuildでJSX構文検証）で構文検証済み。Chrome拡張機能が未接続のため実ブラウザでの表示幅・折り返しの確認は未実施。
- 顧問先マスタの詳細表示（`CustomerDetailModal`）のCC欄を名前・メールで別行表示に変更、コピーボタンをメールのみコピーに修正（2026-09-18）。
  - 背景：詳細表示のCC①②③欄が「名前　メール」を1行に連結した文字列（`formatCcDisplay`）で表示され、コピーボタンもその連結文字列をコピーしていたため、名前とメールアドレスを分けて確認・コピーしづらかった。
  - 修正内容：`CustomerDetailModal`のCC①②③行を、`fields`配列駆動の共通レンダリング（1行に値＋コピーボタン）から専用のレンダリングに変更。各CC行を「名前：〇〇」「メール：〇〇　[コピー]」の2行表示にし、コピーボタンは`item.CC{n}メール`（メールアドレスのみ）をコピーするよう変更。名前行にはコピーボタンを付けない。
  - 実装：既存の`fields`配列を`fieldsBefore`（業種〜担当者メール）と`fieldsAfter`（決算月〜東京SR特別加入）に分割し、その間にCC①②③専用のブロックを挿入。共通の1行レンダリングは`renderField`関数に切り出し、`fieldsBefore`/`fieldsAfter`はこれまで通り共通レンダリングを使用。
  - 影響範囲：`CustomerDetailModal`（詳細表示）のみ。編集モーダル（`MasterItemModal`）のCC入力欄・一覧テーブルのCC列（`joinCcNames`）・納品メール下書き（`DeliveryMailModal`、`formatCcDisplay`を引き続き使用）・データ構造には変更なし。
  - Babel（`@babel/preset-react`相当、esbuildでJSX構文検証）で構文検証済み。Chrome拡張機能が未接続のため実ブラウザでの表示・コピー動作の確認は未実施。
- 顧問先マスタの編集モーダルのレイアウトを前回修正前に戻す（CC横並びは維持）（2026-09-18）。
  - 背景：直前の修正で編集モーダルを2カラム・幅1040pxのコンパクトなレイアウトに変更したところ、「項目が密集して入力しにくい」との指摘があったため、モーダルのレイアウトのみ差し戻し。
  - 戻した内容：`MasterItemModal`（顧問先タブ）の幅を`min(1040px, 94vw)`→760pxに戻し、2カラム構成（`grid grid-cols-2 gap-x-8`）を廃止して元の単一カラム・縦積み構成（`mb-3`区切り、項目順：業種→代表者名/メール→宛名/担当者メール→CC①②③→契約サービス→事務所担当者→36協定起算日/その他労使協定→決算月/特別加入）に戻した。縦スクロールは許容。
  - 維持した内容：①CC①②③の入力欄は「名前」「メール」を横並び（`flex`）で表示する形式を維持（ラベル「CC①」は他項目と同様に上に表示し、その下に名前・メール入力欄を横並びで配置）。②顧問先一覧テーブルのCC列（名前のみ表示、`joinCcNames`）・代表者名列削除は今回のモーダル修正とは無関係のため変更なし。
  - 影響範囲：`MasterItemModal`の顧問先タブ用レイアウトのみ。手続き・担当者タブのモーダル、一覧テーブル、データ構造・保存ロジックには変更なし。
  - Babel（`@babel/preset-react`相当、esbuildでJSX構文検証）で構文検証済み。Chrome拡張機能が未接続のため実ブラウザでの見た目確認は未実施。
- 顧問先マスタの編集モーダルのレイアウト改善とCC表示を名前のみに変更（2026-09-18）。
  - 修正1（編集モーダルのレイアウト改善）：`MasterItemModal`（顧問先タブ、`showNo`時のみ）の幅を760px→`min(1040px, 94vw)`に拡大し、業種以下の項目を2カラムレイアウトに再構成（左列：業種・代表者名/メール・宛名/担当者メール・契約サービス・事務所担当者、右列：CC①②③・36協定起算日/その他労使協定・決算月/特別加入）。スクロールなしで全項目が収まるよう調整。手続き・担当者タブ（`showNo`なし）のモーダル幅・レイアウトは変更なし。
  - 修正2（CC欄の表示形式変更）：編集モーダル内のCC①②③を、ラベルが入力欄の上に来る縦積み表示から、「CC①　名前：[...]　メール：[...]」の横一列表示（`flex`）に変更。
  - 修正3（一覧画面のCC列を名前のみに変更）：顧問先一覧テーブルのCC列を、メールアドレス表示（`joinCcEmails`）から名前のみの表示（新規`joinCcNames`関数、CC①②③の名前を「、」区切りで結合）に変更。
  - 修正4（一覧画面の列整理）：顧問先一覧テーブルから「代表者名」列を削除（利用者確認の上、宛名列と重複するため削除を選択）。詳細表示（`CustomerDetailModal`）・編集フォームの代表者名欄は変更なし（データ自体は保持）。
  - 影響範囲：`MasterItemModal`の表示・レイアウトと`MasterScreen`の一覧テーブル列構成のみ。データ構造（フィールド名・`MASTER_VERSION`）・保存ロジック・Excel出力・納品メール下書き・他画面には変更なし。
  - Babel（`@babel/preset-react`相当、esbuildでJSX構文検証）で構文検証済み。Chrome拡張機能が未接続のため実ブラウザでのレイアウト崩れ・スクロール有無の確認は未実施。
- 顧問先マスタのCC欄を3名分（名前＋メール）に拡張・事務所担当者欄を追加（2026-09-18）。
  - 修正1（CC欄拡張）：単一の「CCメール」（文字列、「、」区切り）を廃止し、`CC1名`/`CC1メール`/`CC2名`/`CC2メール`/`CC3名`/`CC3メール`の3組（各任意入力）に変更。編集フォーム（`MasterItemModal`）・詳細表示（`CustomerDetailModal`）・一覧テーブル・Excel一括出力（`CUSTOMER_MASTER_EXCEL_HEADERS`/`buildCustomerMasterExportRows`）・納品メール下書き（`DeliveryMailModal`のCC欄・Gmail下書きURL）を対応する形式に更新。
  - データ移行：`MASTER_VERSION`を4→5に更新し、`migrateCustomersV4toV5`を新規追加。既存の「CCメール」の値はそのままCC①のメール（`CC1メール`）に移行（複数アドレスが「、」区切りで入っていた場合も分割せずCC①へ一括移行、名前は空欄）。ローカル保存（`loadMasters`）・Firebase初回同期（`App`内`loadFromDB`相当の処理）の両方でv3→v5・v4→v5のマイグレーションを通すよう修正。
    - 重要：修正前のFirebase初回同期ロジックは`loadedMasters.version === MASTER_VERSION || loadedMasters.version === 3`のみを許容しており、`MASTER_VERSION`を単純に5へ上げると本番のv4データ（現行データはこの版）が条件に一致せず、初期化用のローカルデフォルト顧客データでFirebase上のmastersを上書きしてしまうバグを内包していた。`[3, 4, MASTER_VERSION].includes(loadedMasters.version)`に修正し、v3・v4のいずれからもv5へ正しく移行してから書き戻すよう変更済み（本番データの上書き事故を回避）。
  - 修正2（事務所担当者欄追加）：顧問先マスタに`事務所担当者`（文字列、担当者マスタの氏名を保持）を追加。編集フォームに「契約サービス」欄の下へ、担当者マスタ（`masters.assignees`）から選択するプルダウンを配置（案件管理の「担当者」欄と同じUIパターン）。詳細表示・一覧テーブルにも表示列を追加。`MasterScreen`から`MasterItemModal`へ`assignees`propを新規で渡すよう変更。
  - 影響範囲：顧問先マスタのCC・事務所担当者関連の表示・入力・出力ロジックのみ。案件管理・請求集計・月変チェック・年更算定・給与計算・提携先マスタ・shogu-app連携データには変更なし。
  - Babel（`@babel/preset-react`相当、esbuildでJSX構文検証）で構文検証済み。Chrome拡張機能が未接続のため実ブラウザでの入力・表示・移行動作の確認は未実施。
- 給与計算「月次データ入力」ポップアップの月変チェック（改定月）を「月額変更届」グループとして折りたたみ表示に追加（2026-09-06）。
  - 修正前：手続きマスタのグループ設定に基づく関連案件（`cases`）はグループ別折りたたみ表示になっていたが、月変チェック（`checks`、`relatedChecks`）はグループ化されず、折りたたみ一覧の下に常時フラット表示されていた。
  - 修正後：`PayrollMonthModal`の表示ループを`PROCEDURE_GROUPS.map(...)`から`[...PROCEDURE_GROUPS, '月額変更届'].map(...)`に変更し、末尾に「月額変更届」という専用グループを追加。このグループのみ`items`に`groupedCases[g]`ではなく`relatedChecks`を使用（`isCheckGroup`フラグで分岐）し、他のグループと同じ「▶/▼ グループ名 件数」の見出し・クリック開閉（`expandedGroups` state）に統一。展開時の子要素の表示内容（「月額変更届｜改定月：〇〇｜手続き対象者」）は変更前と同一。
  - 手続きマスタのグループ「給与変更・賞与支給」とは別枠の専用グループ名（「月額変更届」）として扱う設計（`PROCEDURE_GROUPS`定数自体は変更していない）。
  - 影響範囲：`PayrollMonthModal`内の表示ループのみ。`relatedCases`・`relatedChecks`のデータ取得ロジック・`groupedCases`のグルーピング判定・データ構造・他画面には変更なし。
  - Babel（`@babel/preset-react`、本番と同じJSXトランスフォーム）で構文検証済み。Chrome拡張機能が未接続のため実ブラウザでの折りたたみ・展開動作の確認は未実施。
- 給与計算画面の年間全体イベント表示をバッジ風デザインに改善（2026-09-06）。
  - 修正内容：`PayrollScheduleScreen`の「全体イベント」表示ブロックの各バッジのスタイルを変更。
    - 全イベント共通：角丸（`borderRadius:'9999px'`）のバッジ形状は共通。
    - 当月・翌月（`state:'current'`または`'next'`）：白背景（`#ffffff`）・オレンジ枠線（`1.5px solid #ea580c`）・オレンジ文字（`#ea580c`）・太字。塗りつぶしなし（アウトライン表示）。従来は当月＝オレンジ塗りつぶし白文字、翌月＝薄いオレンジ塗りつぶしと当月・翌月で見た目を分けていたが、本修正で当月・翌月を同じ「白背景・オレンジ枠線」の控えめな強調表示に統一。
    - それ以外（`state:'none'`）：薄いグレー背景（`#f3f4f6`）・グレー枠線（`1px solid #d1d5db`）・グレー文字（`#6b7280`）の通常バッジ。
  - 影響範囲：色・枠線・背景・角丸などスタイル指定のみ。当月徴収・翌月徴収の分割表示ロジック（`getPayrollEventDisplayItems`）・強調判定ロジック（`getPayrollEventState`）・ラベル文言・他画面・データ構造には変更なし。
  - Babel（`@babel/preset-react`、本番と同じJSXトランスフォーム）で構文検証済み。Node.jsで9月・3月表示時のハイライト対象（算定基礎届／社会保険料率改定の当月・翌月ペアのみ強調、他はグレー）が依頼の表示イメージと一致することを確認済み。Chrome拡張機能が未接続のため実ブラウザでの表示確認は未実施。
- 給与計算画面の年間全体イベント強調表示の色を青系からオレンジ系に変更（2026-09-06）。
  - 理由：右上の「年間一覧」「顧問先追加」ボタン（青系）と強調表示の色が被り目立たなかったため。
  - 修正内容：`PayrollScheduleScreen`の「全体イベント」表示ブロックで、当月（`state:'current'`）の色を青背景・白文字（`background:'#2563eb'`）からオレンジ背景・白文字（`background:'#ea580c'`、border同色）に、翌月（`state:'next'`）の色を薄い青背景・青文字（`background:'#dbeafe'`,`color:'#1d4ed8'`,border`#93c5fd`）から薄いオレンジ背景・オレンジ文字（`background:'#ffedd5'`,`color:'#c2410c'`,border`#fdba74`）に変更。非該当（`state:'none'`）のグレー文字表示は変更なし。
  - 影響範囲：色指定のみ。当月徴収・翌月徴収の分割表示ロジック（`getPayrollEventDisplayItems`）・強調判定ロジック（`getPayrollEventState`）・他画面・データ構造には変更なし。
  - Babel（`@babel/preset-react`、本番と同じJSXトランスフォーム）で構文検証済み。Chrome拡張機能が未接続のため実ブラウザでの表示色確認は未実施。
- 給与計算画面の年間全体イベント表示を、当月徴収・翌月徴収の分割表示と強調デザインに変更（2026-09-06）。
  - 修正1（当月徴収・翌月徴収の分割表示）：「社会保険料率改定」（3月）・「算定基礎届適用」（9月）の2イベントを、`PAYROLL_EVENTS`に追加した`split:true`フラグで判定し、`getPayrollEventDisplayItems`（新規関数）で「3月 社会保険料率改定（当月徴収）」「4月 社会保険料率改定（翌月徴収）」、「9月 算定基礎届適用（当月徴収）」「10月 算定基礎届適用（翌月徴収）」の2件ずつに分解して表示。他の4イベント（雇用保険料率改定・住民税更新・最低賃金改定・年末調整還付）は分割せず従来通り1件表示。
  - 強調表示の判定：分割対象イベントは、該当月（例：9月）を表示中は分割後の2件とも強調表示（該当月側は「当月」、翌月側は「翌月」）、翌月（例：10月）を表示中も同様に2件とも強調表示（翌月側が「当月」、該当月側が「翌月」）となるよう`getPayrollEventState`（`isPayrollEventActive`を置き換えた新規関数、該当月なら`'current'`・翌月なら`'next'`・それ以外は`'none'`を返す）で判定。Node.jsで3月・4月・9月・10月・1月・2月・11月の計7パターンをシミュレーションし、依頼の強調表示仕様（9月表示で9月当月徴収・10月翌月徴収が両方強調）と一致することを確認済み。
  - 修正2（強調表示デザイン）：当月（`state:'current'`）は青背景（`#2563eb`）・白文字、翌月（`state:'next'`）は薄い青背景（`#dbeafe`）・青文字（`#1d4ed8`）、非該当（`state:'none'`）は従来通りグレー文字（`#93a3b8`、背景なし）に変更。
  - 影響範囲：`PayrollScheduleScreen`の「全体イベント」表示ブロックのみ。イベントの判定対象月・他画面（案件管理・請求集計・月変チェック・年更算定）・データ構造には変更なし。
  - Babel（`@babel/preset-react`、本番と同じJSXトランスフォーム）で構文検証済み。Chrome拡張機能が未接続のため実ブラウザでの表示・強調色の確認は未実施。
- 給与計算「月次データ入力」ポップアップの関連案件表示を、手続きマスタのグループ設定に基づくグループ別折りたたみ表示に変更（2026-09-06）。
  - 修正前：関連案件（当月・前月の日付フィールドに一致した案件）を手続き名・日付・メモのフラットな一覧で表示していた。
  - 修正後：`PayrollMonthModal`に`masters`propを追加し、各関連案件の`procedure`名を`masters.procedures`で突合してグループ（`グループフィールド`）を判定、`PROCEDURE_GROUPS`定数の順序で「▶ グループ名 件数」の折りたたみ見出しを表示。見出しクリックで開閉（`expandedGroups` state、初期状態は全グループ折りたたみ）。展開すると子要素に「手続き名｜日付｜メモ」を表示（メモの表示形式は変更前と同一）。手続きマスタにグループ未設定、またはマスタに存在しない手続き名の場合は「その他」グループにまとめる。
  - 月変チェック（`relatedChecks`、月額変更届）の表示はグルーピング対象外のまま、従来通りグループ一覧の下に常時表示。
  - データ構造・案件管理画面・Firebaseへの書き込みロジックへの変更なし。表示ロジックのみの変更。
  - Babel（`@babel/preset-react`、本番と同じJSXトランスフォーム）で構文検証済み。Chrome拡張機能が未接続のため実ブラウザでの開閉動作・グルーピング結果の確認は未実施。
- 請求集計「CSVダウンロード」（かつ・かいしゅう用）からシステム利用料（内訳コード27）の行を除外（2026-09-03）。
  - 来月からポータルの請求業務ウィザードSTEP1でシステム利用料を自動計算し、STEP3のCSVに含める運用に変更するため、task-app側のCSVで二重出力にならないよう対応。
  - `index.html` の `BillingSummaryScreen` 内 `handleDownloadCSV` で、内訳コード'27'を追加する行（`add('27', r.システム利用料)`）を削除。他の内訳コード（01・02/03/28・26・36・56）の出力条件は変更なし。
  - 請求集計画面のシステム利用料入力欄・表の「システム利用料」列表示（`r.システム利用料`を参照する箇所）は変更していない。画面上は従来通り金額が表示される。
  - `billingSnapshots`・`payrollRates`等の既存データ・Firebase上の値には一切影響しない（CSV出力ロジックのみの変更）。
  - 実ブラウザでの動作確認は未実施（Chrome拡張機能未接続のため）。
- 顧問先マスタの「担当者名」を「宛名」に表示名変更・納品メール下書きの「様」自動付与を削除（2026-08-28）。
  - 修正1（表示名変更）：顧問先マスタの編集フォーム（`MasterItemModal`）・詳細表示（`CustomerDetailModal`）・一覧テーブル（`MasterScreen`、顧問先タブのみ）のラベルを「担当者名」から「宛名」に変更。データの内部キー（Firebase上のフィールド名`担当者名`）・保存ロジックは変更なし（表示ラベルのみの変更で、データ移行は不要）。案件管理の社内担当者（`assignee`）欄・提携先マスタ（`PartnerItemModal`/`PartnerDetailModal`）・shogu-appへの連携データ（`担当者名`キーのまま）は別概念のため対象外、変更していない。
  - 修正2（「様」自動付与の削除）：`DeliveryMailModal`の`greetingLines`から`.map(s => \`${s}様\`)`を削除。マスタの宛名欄に登録された値をそのまま下書き本文・プレビューに使用する（「様」を含めるかどうかはマスタ登録時の入力に委ねる）。
  - プレースホルダー変更：宛名欄のプレースホルダーを「メールに表示する名前。複数は「、」区切り（例：田中、浅野）」から「メールに表示する名前。複数は「、」区切り（例：田中様、浅野様）」に変更（「様」を登録時に含める運用に合わせた例示）。
  - 影響範囲：顧問先マスタの表示ラベル・納品メール下書きの宛名表示ロジックのみ。データ構造・保存先・CCメール/フォルダ名/署名のロジックには変更なし。
- 顧問先マスタ編集フォームの担当者名プレースホルダーを修正（2026-08-28）。
  - 修正前：「苗字のみ。複数人は「、」区切り（例：田中、浅野）」。
  - 修正後：「メールに表示する名前。複数は「、」区切り（例：田中、浅野）」。納品メール下書きの宛名が苗字自動抽出からマスタ登録値そのまま使用に変更されたこと（直前の修正）に合わせ、「苗字のみ」という表現をやめ「メールに表示する名前」に変更。
  - 担当者メール（To）・CCメールのプレースホルダーは変更なし（それぞれ「担当者メールアドレス」「複数可。「、」区切り（例：a@example.com、b@example.com）」のまま）。
  - 影響範囲：顧問先マスタ編集フォームのプレースホルダー文言のみ。ラベル・保存ロジック・データ構造には変更なし。
- 納品メール下書きの宛名を、担当者名からの苗字自動抽出からマスタ登録値をそのまま使用する方式に変更（2026-08-28）。
  - 修正前：`extractSurnames`関数が担当者名の各セグメント（「、」区切り）の先頭2文字を苗字とみなして自動抽出していた（例：「田中里子」→「田中」）。3文字以上の苗字は誤って切り詰められる問題があった。
  - 修正後：自動抽出処理を全廃し、`splitNames`関数に置き換え。「、」区切りで分割するだけで、文字数による切り詰めは一切行わずマスタ登録値をそのまま使用する。例：マスタ登録「田中」→「田中様」、「田中、浅野」→「田中様」改行「浅野様」、「Maria Santos」→「Maria Santos様」（英語名でも切り詰めない）。
  - 前提：顧問先マスタの担当者名欄は既に「苗字のみ登録・複数は「、」区切り」の運用に変更済み（2026-08-28の別修正）のため、自動抽出に頼らずマスタの値をそのまま使う方が正確という判断。
  - 影響範囲：納品メール下書き機能の宛名表示（下書き本文・ポップアップ内プレビュー）のみ。CCメール・フォルダ名・署名・Gmail下書き作成URLのロジックには変更なし。
- 【データ構造変更・利用者承認済み】顧問先マスタの担当者②を廃止しCCメールを新設（2026-08-28）。
  - 変更前：担当者名①・担当者メール①・担当者名②・担当者メール②の4項目構成。
  - 変更後：担当者名（複数人は「、」区切りで手入力、苗字のみ推奨）・担当者メール（To、変更なし）・CCメール（新設、複数可・「、」区切り）の3項目構成。担当者名②・担当者メール②は編集フォーム・詳細表示・一覧テーブル・Excel出力（`CUSTOMER_MASTER_EXCEL_HEADERS`）から廃止。
  - データ移行：`MASTER_VERSION`を3→4に変更し、`migrateCustomersV3toV4`関数を新規追加。既存の担当者メール②の値をCCメールへ移動、担当者名②の値を担当者名に「、」で結合（例：担当者名①「田中里子」＋担当者名②「浅野」→担当者名「田中里子、浅野」）。移行後、担当者名②・担当者メール②のキー自体をデータから削除。移行はNode.jsで代表的な5パターン（両方あり・メール②のみ・名前②のみ・②なし・空データ）を単体テストし、期待通りの結果および2回連続実行しても結果が変わらない（冪等性）ことを確認済み。
  - 移行のタイミング：アプリ起動時（`loadFromDB`のFirebase初回読込時、および`loadMasters`のlocalStorageキャッシュ読込時）に自動実行し、**Firebase上の本番データも書き換える**（既存の「スポット→その他（直接入力）」移行と同じ方式）。移行前にFirebase側の`masters.version`が3であることを確認し、移行後は4になる。
  - `loadFromDB`のバージョン判定に重要な修正：`MASTER_VERSION`を4に上げたことで、Firebase上のv3データがそのままでは「バージョン不一致」と誤判定され、既存データがローカルの初期値で上書きされてしまうバグになり得たため、v3データも正規のデータとして受理し移行処理を通すよう条件式を修正（`loadedMasters.version === MASTER_VERSION || loadedMasters.version === 3`）。
  - 納品メール下書きへの反映：`DeliveryMailModal`の宛先（To）は`master.担当者メール`のまま変更なし。CCは`master.CCメール`を取得し、内部の「、」区切りをGmail用の半角カンマ区切りに変換した上でGmail下書き作成URLの`cc`パラメータに設定。ポップアップ内プレビューにもCCの値を表示（未登録時は非表示）。
  - 影響範囲：顧問先マスタのデータ構造・編集フォーム・詳細表示・一覧テーブル・Excel出力・納品メール下書き機能。他の画面（案件管理・請求集計・月変チェック・年更算定・給与計算）には変更なし。
- 全ポップアップ・モーダルの外側クリックによる誤閉じを修正（2026-08-28）。
  - 修正前：`納品メール下書き作成`ポップアップ（`DeliveryMailModal`）以外の全モーダル・ポップアップ（案件編集、月変チェック編集、年更・算定編集、給与単価設定、給与計算月次データ入力、顧問先マスタ編集、提携先編集、確認ダイアログ、36協定アラート等、`index.html`内の`modal-overlay`を使う全16箇所）が、背景（外側）クリックで閉じる作りになっていた。入力途中のフォームデータが誤って消える可能性があった。
  - 修正後：全ての`<div className="modal-overlay">`から`onClick={e=>e.target===e.currentTarget&&onClose()}`等の背景クリックで閉じる処理を削除。各モーダルの「✕」ボタン・「キャンセル」ボタン・「閉じる」ボタン（既存のまま、削除していない）を押したときのみ閉じる。Node.jsの正規表現置換で該当16箇所全てを一括修正し、置換件数が期待通り16件であることを確認、置換後に`modal-overlay`へのonClickが0件であることも確認済み。
  - 影響範囲：モーダルの閉じ方（外側クリックの無効化）のみ。各モーダルの入力項目・保存ロジック・Firebaseへの書き込み内容には変更なし。全モーダルに「✕」または「キャンセル／閉じる」ボタンが残っていることを確認済みのため、閉じられなくなるモーダルはない。
- 納品メール下書き機能を2点修正（2026-08-28）。
  - 修正1（フォルダ名入力欄のプレースホルダー削除）：`DeliveryMailModal`のフォルダ名`<input>`から例示テキスト（「例：20260827_株式会社天山工務店_田辺 伶生様_雇用保険喪失(離職票あり)」）を削除。入力欄は空欄表示になる。
  - 修正2（担当者名の区切り文字を「、」のみに変更）：`extractSurnames`の区切り文字を「、」のみに変更。「，」「,」は区切りとして扱わなくなった（該当データがあれば1件の氏名の一部として扱われる）。「・」も区切りとして扱わなくなり、氏名の一部として扱われる（例：「渡辺・黒川」は1人の氏名とみなされ、先頭2文字の「渡辺」のみ苗字として抽出される。以前は「・」区切りとして`['渡辺','黒川']`の2人として扱っていた）。
  - 影響範囲：納品メール下書き機能のみ。「・」を区切りとして使っていたデータがあれば宛名の抽出結果が変わる点に注意（下書き確認時に修正可能）。
- 納品メール下書きの署名テキストの行間を修正（2026-08-28）。
  - 修正前：罫線のすぐ下に全角スペースのみの行を1行挟むのみで、「社会保険労務士齊藤事務所」「住所以下5行」「キャッチコピー」の間には空行がなかった。
  - 修正後：罫線の下・「社会保険労務士齊藤事務所」の下・住所以下5行の下（キャッチコピーの上）にそれぞれ空行を挟む形式に変更（`DELIVERY_MAIL_SIGNATURE`定数を全面差し替え）。Node.jsでの文字列比較により指定書式との一致を確認済み。
  - 影響範囲：署名テキストの行間（空行の位置）のみ。文言・罫線記号・チルダ等の文字自体は変更なし。
- 納品メール下書きの署名テキストを修正（2026-08-28）。
  - 修正前：「--」区切り線＋「社会保険労務士齊藤事務所」の後に空行を挟んで住所以下を記載する簡易的な署名だった。
  - 修正後：`＿/`の罫線で上下を囲み、罫線のすぐ下に全角スペースのみの行を挟んで「社会保険労務士齊藤事務所／住所／TEL／FAX／Mail／Web／キャッチコピー（～社会保険手続きや給与計算のご相談は当事務所まで～）」を1行ずつ記載する指定の書式に変更（`DELIVERY_MAIL_SIGNATURE`定数を全面差し替え）。文字は指定どおり全角の罫線記号・全角チルダ（～）を使用。
  - 影響範囲：署名テキストの内容のみ。本文の他の部分（宛名・フォルダ名欄・Gmail下書き作成方法等）には変更なし。
- 納品メール下書きポップアップの外側クリックによる誤閉じを修正（2026-08-28）。
  - 修正前：ポップアップの外側（背景）をクリックすると`onClose`が呼ばれて閉じてしまい、入力中のフォルダ名・説明書きが失われていた。
  - 修正後：`DeliveryMailModal`の`modal-overlay`から`onClick={e=>e.target===e.currentTarget&&onClose()}`を削除。ヘッダーの「✕」ボタンと下部の「閉じる」ボタンを押したときのみ閉じる（＝ボタンを明示的に押すまで入力内容は保持される。モーダルはコンポーネントのアンマウントで閉じる作りのため、フォームstate自体はボタンを押すまで保持され続ける）。
  - 影響範囲：納品メール下書きポップアップのみ。他のモーダル（届出編集・案件編集等）の外側クリック挙動には触れていない。
- 納品メール下書き機能を3点修正（2026-08-28）。
  - 修正1（自動ポップアップの削除）：ステータスを「納品済」に変更した瞬間にポップアップを自動表示していた挙動を削除。`CaseListScreen`のステータス`<select>`の`onChange`から`setMailPopupCase`呼び出しを削除し、以後は「納品済」行に常時表示される「📧」ボタンを押したときのみポップアップが開く（📧ボタン自体・表示条件は変更なし）。
  - 修正2（フォルダ名の複数入力）：`DeliveryMailModal`の`folderName`（単一文字列）を`folders`（`{name, note}`の配列、初期値1件）に変更。「＋ フォルダを追加」ボタンで入力欄を追加、各欄に2件目以降は「削除」ボタン（1件のみの場合は非表示）。各フォルダに任意の説明書き用`<textarea>`（空欄可）を追加。本文には`フォルダ名：{name}`の下に説明書きがあれば改行して追記し、フォルダごとに空行区切りで並べる（名前が空欄のフォルダは本文に含めない）。
  - 修正3（署名の自動追加）：新規定数`DELIVERY_MAIL_SIGNATURE`（社会保険労務士齊藤事務所の住所・電話・FAX・メール・Webサイト・キャッチコピーを含む固定署名）を本文末尾に自動追記。
  - Gmail側の自動署名との重複について：本アプリはGmail APIのOAuth連携ではなく`https://mail.google.com/mail/?view=cm&fs=1&...&body=...`のコンパクトURLでGmail作成画面を開く方式のため、本文パラメータで渡した署名とは別に、利用者のGmailアカウント側で「署名」機能（設定 → 全般 → 署名）が有効な場合、Gmailが独自にもう一つ署名を追加し二重表示になる可能性がある。Chrome拡張機能が未接続で実際のGmail作成画面を開いての確認ができなかったため、次回ログイン時に実際に下書きを開いて二重になっていないか確認し、二重になっている場合はGmail設定側の署名を無効化するか、下書き作成時に手動で片方を削除する運用とする（URLパラメータのみでGmail側の自動署名設定を制御する方法はない）。
  - 影響範囲：納品メール下書き機能のみ。会社名/宛先メール取得ロジック・苗字抽出ロジック・「📧」ボタンの表示条件・他画面には変更なし。
- 納品メール下書きの宛名形式を修正（2026-08-28）。
  - 修正前：複数の担当者がいる場合「田中・浅野様」のように1行にまとめて表示・下書き作成していた。
  - 修正後：担当者ごとに改行し、それぞれに「様」をつける形式に変更（例：「田中様」改行「浅野様」）。担当者が1人または未登録の場合は従来通り1行（未登録時は「様」のみ）。
  - 実装：`extractSurnames`の戻り値を結合済み文字列（`'田中・浅野'`）から苗字の配列（`['田中','浅野']`）に変更。呼び出し側の`DeliveryMailModal`で`greetingLines = surnames.map(s => `${s}様`)`を作り、下書き本文・ポップアップ内プレビューの両方を`greetingLines.join('\n')`／1行ずつの`<div>`で表示するよう変更。
  - 影響範囲：納品メール下書き機能のみ。フォルダ名入力・Gmail下書き作成URL・会社名/宛先メールの取得ロジック・他画面には変更なし。
- 案件管理（手続き一覧）に納品メール下書き自動作成機能を追加（2026-08-28）。
  - トリガー：ステータスを「納品済」に変更した瞬間にポップアップ（`DeliveryMailModal`）を自動表示。加えて、ステータスが「納品済」の行（取消済みを除く）にはステータス列の右に「📧」ボタンを常時表示し、いつでも同じポップアップを再表示できる。
  - ポップアップの内容：フォルダ名の入力欄（テキスト、コピペ可）、「Gmailに下書きを作成」ボタン、「閉じる」ボタン。会社名・宛先メールのプレビューも表示。
  - データ取得：`caseData.customerName`で`masters.customers`（顧問先マスタ）を突合し、会社名（`name`）・担当者メール（`担当者メール`）を自動取得。担当者メールが未登録の場合は空欄のまま下書きを作成（画面上にも「担当者メール未登録」の注記を表示）。
  - 担当者名の苗字抽出：新規ヘルパー関数`extractSurnames`を追加。「、」「,」区切りは1人ずつの氏名とみなし先頭2文字を苗字とみなす簡易推定（例：「田中里子、浅野」→['田中','浅野']）。「・」区切りは元データ側で既に氏名単位に分かれているとみなしそのまま使う（例：「渡辺・黒川」→['渡辺','黒川']）。3文字以上の苗字は誤って2文字に切られる場合があるため、下書き作成後にGmail側で内容を確認・修正する運用を前提とする。
  - Gmail下書き作成方法：Gmail APIのOAuth連携は行わず、`https://mail.google.com/mail/?view=cm&fs=1&to=...&su=...&body=...`のコンパクトURL形式でGmailの作成画面を新規タブで開く方式（`window.open`）。件名・本文・宛先を自動入力した状態でGmail作成画面が開き、送信しなければGmail側で自動的に下書き保存される。
  - 実装箇所：`CaseListScreen`（`index.html`内、案件管理画面）のステータス`<select>`の`onChange`に、`納品済`への変化を検知する分岐を追加（既存の保存処理・他ステータスの遷移ロジックは変更なし）。ステータス列の右に新規カラム（幅36px、ヘッダーは空欄）を追加し、`📧`ボタンを配置。
  - 既存機能への影響：ステータスの選択肢・保存先（Firebase `cases`）・他画面（請求集計・月変チェック・年更算定・給与計算・マスタ管理）には変更なし。ポップアップを閉じても案件データやステータスの保存には影響しない（ポップアップはあくまで下書き作成の補助UIで、Firebaseへの追加書き込みは行わない）。
- 請求集計「案件請求」列の集計から取消済み（`cancelled:true`）案件が漏れて含まれる不具合を修正（2026-08-22）。
  - 経緯：株式会社カノエの請求集計（2026年8月）に、案件管理側では0件のはずの「案件請求」が1件・金額付きで表示される不具合の調査依頼。
  - 原因：`rows`計算内の「案件請求」集計フィルター（`cases.filter(...)`）に、他の集計（月変請求・年更算定請求）では存在する `!c.cancelled` チェックが漏れていた。実データ確認により、取消済み・`status:'請求済'`の案件（例：カノエ id 44、正社員転換、取消済みだがステータスが請求済のまま）が誤って件数・金額に混入していたことを確認。
  - 修正：`cases.filter(c=>c.targetMonth===sel && c.status==='請求済' && ...)` に `!c.cancelled` を追加し、月変請求・年更算定請求と同じ条件に統一。
  - `payrollRates`（給与単価設定）は「案件請求」列と無関係であり、不正データの有無は確認不要と判断（コードレビューで確認済み）。
  - 影響範囲：全顧問先の「案件請求」集計（取消済みかつステータスが更新されずに残っている案件があるケース全般）。既に「確定済み」の月はスナップショットが固定表示されるため、この修正後も画面上の数値を反映するには対象月の「確定解除」が必要な場合がある。
- 一括引き継ぎボタンで賞与請求額が意図せず自動計算される不具合を修正（2026-08-21）。
  - 原因：「給与計算（段階単価）」は仕様通り一括引き継ぎ対象だが、この段階単価は賞与請求額の自動計算（`賞与固定金額`が未設定の場合のフォールバック、`給与人数×単価`と同じ仕組みを`賞与人数`にも適用）にも共用されていたため、賞与人数が入力済みの顧問先で意図せず賞与請求額が計算・表示されていた。実際にFirebaseへ書き込まれる`月別設定`には`追加件数`・`追加金額`・`賞与固定金額`は含まれておらず、これらのフィールド自体が引き継がれる不具合ではなかった（表示上の計算結果の問題）。
  - 修正：`handleBulkInherit`が作成する当月設定に `賞与単価除外: true` フラグを追加。`rows`計算（`賞与人>0`の分岐）でこのフラグが立っている場合、段階単価による賞与請求額の自動計算をスキップする（`賞与固定金額`が明示的に設定されていれば従来通りそちらを優先）。
  - このフラグは`PayrollRateModal`で個別に保存すると自然に消える（モーダルの保存処理はフォーム値から`setting`を新規構築するため、フラグを引き継がない）。個別に確認・保存した月は通常通り段階単価から賞与請求額が計算される。
  - 給与請求額の計算・給与計算段階単価の一括引き継ぎ自体には変更なし。
- 請求集計画面に「← 前月を一括引き継ぎ」ボタンを追加（2026-08-21）。
  - `BillingSummaryScreen` の `handleBulkInherit`。対象年月未確定（`!isConfirmed`）の場合のみ「確定する」の左に表示。
  - 対象項目：労務顧問・給与計算段階単価（5区分）・給与計算固定金額・処遇改善加算・システム利用料。追加案件（件数・請求額）・賞与計算固定金額は対象外（従来通り月ごとにリセット）。
  - 各顧問先ごとに `payrollRates[name].月別設定[当月]` が既に存在する場合はスキップ（上書きしない）。前月の設定が無ければ旧フラット構造にフォールバックし、それも無ければ何もしない。
  - `PayrollRateModal` の保存処理と同じ規則（`inherited()` 相当のロジック、旧フラット構造キーの除去）を全顧問先分まとめて適用し、`setPayrollRates` を1回だけ呼び出して保存。
  - 実行前に `window.confirm` で確認、実行後に処理件数を画面上のバナーで4秒間表示。
  - 顧問先マスタ・請求集計の金額計算ロジック・Firebaseデータ構造への変更なし（既存の `payrollRates` ノードへの書き込みのみ）。
- 給与単価設定モーダル（`PayrollRateModal`）の前月自動引き継ぎ対象を拡大（2026-08-21）。
  - 「給与計算（固定金額・円）」「システム利用料（円）」を、既存の労務顧問・段階単価・処遇改善加算と同じ `inherited()`（当月未保存なら前月設定を自動セット、当月保存済みならその値を使用）に変更。
  - 「追加案件（件数・請求額）」「賞与計算（固定金額）」は従来通り `monthOnly()`（月ごとにリセット）のまま変更なし。
  - 各フィールド横の「前月をコピー」ボタン（給与固定金額・システム利用料）はそのまま維持（削除しても良いが今回は残した）。
  - 顧問先マスタ・請求集計ロジック・Firebaseデータ構造への変更なし。
- AI引継ぎ資料とGit除外設定を整備
- Claude CodeとCodexを対等な開発担当として明記
- Git作業開始・終了、競合停止、利用者承認ルールを統一

## デプロイ・公開状況
- 公開URL: https://hiro-saitoh-sr.github.io/task-app/
- 今回は管理文書とGit除外設定のみを変更し、アプリ、Firebase、GAS、データは変更しない

## 検証結果
- 顧問先マスタの「担当者名」→「宛名」表示名変更・納品メール下書きの「様」自動付与削除（2026-08-28）はesbuild（Babelと同じJSXトランスフォーム）で構文検証済み。`grep`で案件管理の`assignee`・提携先マスタ・shogu-app連携データ内の「担当者名」が変更されていないこと（対象外として意図的に除外）を確認済み。Node.jsで`greetingLines`の挙動（「田中様」「田中様、浅野様」等、マスタ登録値がそのまま使われ「様」が二重に付かないこと）を単体テストし確認済み。Chrome拡張機能が未接続のため実ブラウザでの動作確認は未実施
- 顧問先マスタ編集フォームの担当者名プレースホルダー修正（2026-08-28）はesbuild（Babelと同じJSXトランスフォーム）で構文検証済み。指定文言と`git diff`で1文字単位の一致を確認済み。低リスクな文言修正のため実ブラウザでの動作確認は省略
- 納品メール下書きの宛名を苗字自動抽出からマスタ登録値そのまま使用に変更（2026-08-28）はesbuild（Babelと同じJSXトランスフォーム）で構文検証済み。`splitNames`をNode.jsで指示された3例（「田中」「田中、浅野」「Maria Santos」）およびnull/空文字で単体テストし、いずれも期待通りの結果（切り詰めなし、「、」区切りのみ改行）を確認済み。`grep`で`extractSurnames`・`surnames`への参照が完全に削除されていることも確認済み。Chrome拡張機能が未接続のため実ブラウザでの動作確認は未実施
- 顧問先マスタの担当者②廃止・CCメール新設（2026-08-28）はesbuild（Babelと同じJSXトランスフォーム）で構文検証済み。`migrateCustomersV3toV4`はNode.jsで代表的な5パターン（両方あり・メール②のみ・名前②のみ・②なし・空データ）を単体テストし期待通りの結果を確認、2回連続実行で結果が変わらないこと（冪等性）も確認済み。`grep`で担当者名②・担当者メール②への参照が移行関数内以外に残っていないことも確認済み。Chrome拡張機能が未接続のため実ブラウザでの動作確認・実際のFirebase本番データでの移行結果の確認はいずれも未実施
- 全ポップアップ・モーダルの外側クリック誤閉じ修正（2026-08-28）はesbuild（Babelと同じJSXトランスフォーム）で構文検証済み。置換件数（16件）と置換後の残存件数（0件）をNode.jsで確認し、全16モーダルに「✕」または「キャンセル／閉じる」の明示的な閉じるボタンが残っていることも`grep`で確認済み。Chrome拡張機能が未接続のため実ブラウザでの動作確認は未実施
- 納品メール下書き機能の2点修正（フォルダ名プレースホルダー削除・担当者名区切り文字変更、2026-08-28）はesbuild（Babelと同じJSXトランスフォーム）で構文検証済み。Chrome拡張機能が未接続のため実ブラウザでの動作確認は未実施
- 納品メール下書きの署名テキストの行間修正（2026-08-28）はesbuild（Babelと同じJSXトランスフォーム）で構文検証済み。指定書式（罫線・空行の位置）とNode.jsでの文字列比較により一致を確認済み。デプロイ障害からの復旧確認時（同日18:05頃）にGitHub Pages上の実配信内容でも署名の行間が反映されていることを`curl`で確認済み
- 納品メール下書きの署名テキスト修正（2026-08-28）はesbuild（Babelと同じJSXトランスフォーム）で構文検証済み。指定書式（罫線・全角スペース行・各項目）とNode.jsでの文字列比較により一致を確認済み。Chrome拡張機能が未接続のため実ブラウザでの表示確認は未実施
- 納品メール下書きポップアップの外側クリック誤閉じ修正（2026-08-28）はesbuild（Babelと同じJSXトランスフォーム）で構文検証済み。Chrome拡張機能が未接続のため実ブラウザでの動作確認は未実施
- 納品メール下書き機能の3点修正（自動ポップアップ削除・複数フォルダ名入力・署名自動追加、2026-08-28）はesbuild（Babelと同じJSXトランスフォーム）で構文検証済み。Chrome拡張機能が未接続のため、実ブラウザでの動作確認、および実際のGmail作成画面での署名二重表示の確認はいずれも未実施
- 納品メール下書きの宛名形式修正（2026-08-28）はesbuild（Babelと同じJSXトランスフォーム）で構文検証済み。Chrome拡張機能が未接続のため実ブラウザでの動作確認は未実施
- 納品メール下書き自動作成機能（2026-08-28）はesbuild（Babelと同じJSXトランスフォーム）で構文検証済み。Chrome拡張機能が未接続のため実ブラウザでの動作確認は未実施
- `git fetch origin` 後、作業開始時にローカルと `origin/main` の一致を確認
- 文書変更のためアプリ動作確認は対象外
- `kanshain_v0` は毎日8:30頃に実行され、2026-07-05から2026-07-11まですべて正常完了、エラーなし
- `checkNewNotifications` は毎日8:47頃に実行され、確認した実行履歴はすべて正常完了
- 給与単価設定の前月自動引き継ぎ対応（2026-08-21）はesbuildで構文検証済み。利用者の判断により実ブラウザでの動作確認は未実施
- 請求集計の一括引き継ぎボタン（2026-08-21）もesbuildで構文検証済み。利用者の判断により実ブラウザでの動作確認は未実施
- 一括引き継ぎの賞与請求額除外修正（2026-08-21）もesbuildで構文検証済み。利用者の判断により実ブラウザでの動作確認は未実施
- 「案件請求」取消済み除外修正（2026-08-22）もesbuildで構文検証済み。利用者の判断により実ブラウザでの動作確認は未実施

## 既知の課題
- 給与計算画面の年間全体イベント表示（当月徴収・翌月徴収の分割表示、2026-09-06。強調デザインは同日中に「オレンジ塗りつぶし」→「バッジ風・オレンジ枠線アウトライン」の順に2回改善済み、最新は枠線のみで塗りつぶしなし）の実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - 9月を表示中（またはシステム日付が9月の状態）で、「9月 算定基礎届適用（当月徴収）」「10月 算定基礎届適用（翌月徴収）」の2件のみが、白背景・オレンジ枠線・オレンジ太字のバッジで表示されること
  - 3月を表示中（またはシステム日付が3月の状態）で、「3月 社会保険料率改定（当月徴収）」「4月 社会保険料率改定（翌月徴収）」の2件のみが、白背景・オレンジ枠線・オレンジ太字のバッジで表示されること
  - それ以外のイベント（対象月に該当しないもの）は薄いグレー背景・グレー枠線・グレー文字の通常バッジで表示されること
  - 分割対象外の4イベント（雇用保険料率改定・住民税更新・最低賃金改定・年末調整還付）についても、該当月・翌月に該当する場合は同じオレンジ枠線バッジで強調されること
  - 全体的にバッジ（角丸・枠線あり）の見た目になっており、当月・翌月の強調表示が塗りつぶしではなく枠線のみで控えめに目立っていること
- 【残課題】育児関係で1名に対して複数手続きが表示される問題（2026-09-06時点、未対応）。別途対応が必要。
- 【残課題】業務監査員Ver.0への給与計算イベント通知の追加（Step2、2026-09-06時点、未対応）。
- 給与計算「月次データ入力」ポップアップの関連案件グループ別折りたたみ表示（2026-09-06。月変チェックも同日中に「月額変更届」グループとして折りたたみ対象に追加済み）の実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - 関連案件がある顧問先の月次ポップアップを開き、「▶ グループ名 件数」の見出しがグループごとに表示されること（フラット表示に戻っていないこと）
  - 見出しをクリックすると「▼」に変わり、子要素（手続き名｜日付｜メモ）が展開表示されること。再クリックで折りたたまれること
  - 手続きマスタでグループが未設定、または手続きマスタに登録されていない手続き名の案件が「その他」グループに分類されること
  - メモが登録されている案件は、子表示の末尾にメモの内容がそのまま表示されること
  - 月変チェック（月額変更届）が「▶ 月額変更届 件数」の折りたたみグループとして表示され、クリックで展開すると「月額変更届｜改定月：〇〇｜手続き対象者」が表示されること（グループ一覧の下に常時フラット表示される旧挙動に戻っていないこと）
  - 案件管理画面・他の画面（請求集計・月変チェック・年更算定）の動作に影響がないこと
- 請求集計CSVダウンロードのシステム利用料除外（2026-09-03）の実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - システム利用料が入力されている顧問先を含む月で「CSVダウンロード」を実行し、出力CSVに内訳コード'27'の行が含まれないこと
  - 同じ月の画面上のシステム利用料入力欄・表の「システム利用料」列には従来通り金額が表示されること
  - 他の内訳コード（01・02/03/28・26・36・56）の行がこれまで通り正しく出力されること
  - ポータル側の請求業務ウィザードSTEP1/STEP3側の対応が実際に稼働開始するまでは、システム利用料がどのCSVにも含まれない期間が生じないか運用タイミングを利用者に確認する
- 【重要】顧問先マスタの「担当者名」→「宛名」表示名変更・納品メール下書きの「様」自動付与削除（2026-08-28）の実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - 顧問先マスタの編集フォーム・詳細表示・一覧テーブルで「担当者名」の表記が「宛名」に変わっていること（提携先マスタの「担当者名」表記は変わらないこと）
  - **既存データへの影響**：今回のラベル変更はデータ移行を伴わないが、既存の宛名欄には「様」を含まない値（例：「田中」）が登録されている可能性が高い。この場合、納品メール下書きの宛名が「田中」のまま（「様」なし）で表示される。必要に応じて顧問先マスタの宛名欄を「田中様」のように「様」を含む形に一括または個別で更新する必要がある（本対応では自動移行していない）
  - 宛名欄に「田中様」のように「様」を含めて登録した場合、下書き本文・プレビューにそのまま「田中様」と表示されること（「様」が二重に付かないこと）
  - 宛名欄が複数人（「、」区切り、例：「田中様、浅野様」）の場合、「田中様」改行「浅野様」の2行になること
  - 案件管理の担当者（社内担当者、`assignee`）欄の表記・提携先マスタの表記には影響がないこと
- 【重要】顧問先マスタの担当者②廃止・CCメール新設（2026-08-28）の実ブラウザでの動作確認が未実施。次回ログイン時に以下を必ず確認する必要あり：
  - マスタ管理画面を開いた瞬間に、Firebase上の`masters.version`が3→4に上がり、担当者メール②を登録していた顧問先のCCメール欄に値が正しく移行されていること
  - 担当者名②を登録していた顧問先で、担当者名に「元の担当者名①、担当者名②」の形で結合されていること（結合順序・「、」区切りが正しいか）
  - 顧問先マスタの編集フォームに「担当者名②」「担当者メール②」の欄が表示されなくなり、「担当者名」「担当者メール（To）」「CCメール」の3項目になっていること
  - 顧問先マスタの一覧テーブル・詳細ポップアップでも担当者②の代わりにCCメールが表示されていること
  - 「データ一括出力」のExcel（顧問先マスタシート）で担当者名②・担当者メール②の列が消え、CCメール列が追加されていること
  - 納品メール下書き作成で、CCメールを登録している顧問先の場合、Gmail下書き画面のCC欄に正しくメールアドレスが入っていること（複数件の場合、カンマ区切りで正しく認識されるか）
  - 万一移行結果がおかしい場合は、Firebaseコンソールで`masters`ノードの内容を直接確認し、必要であれば利用者に報告のうえ対応する（本移行は値の削除ではなく再配置のため、担当者名②・担当者メール②の元の値は担当者名・CCメールに残っているはずだが、結合順序や書式が想定と異なる可能性はある）
- 全ポップアップ・モーダルの外側クリック誤閉じ修正（2026-08-28）の実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - 案件編集、月変チェック編集、年更・算定編集、給与単価設定、給与計算月次データ入力、顧問先マスタ編集、提携先編集、確認ダイアログ、36協定アラート、納品メール下書き作成など、代表的なモーダルをいくつか開いて、背景（外側）をクリックしても閉じないことを確認する
  - 各モーダルの「✕」ボタン・「キャンセル」ボタン・「閉じる」ボタンはこれまで通り押せば閉じることを確認する
  - 入力途中のフォームで背景をクリックしても入力内容が消えないことを確認する
- 納品メール下書き機能の2点修正（2026-08-28）の実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - フォルダ名入力欄に例示テキストが表示されず、空欄になっていること
  - 担当者名が「、」区切りの場合は従来通り1人ずつに分割されること
  - 担当者名に「・」が含まれる場合、区切りとして扱われず1人の氏名として扱われる（先頭2文字のみ苗字として抽出される）ようになったこと（実データで該当する顧問先があれば要確認）
- 【解決済み】2026-08-28 17:19頃〜18:05頃、コミット`6d11c72`のGitHub Pagesデプロイが約45分間失敗し続けた。
  - 事象：`.github/workflows/deploy.yml`の「Deploy to GitHub Pages」ジョブが失敗。GitHub Pages API（`/repos/.../pages/deployments`）で確認したところ、デプロイステータスが空文字のまま進行せず、10分のタイムアウトで打ち切られていた。
  - 調査経緯：リポジトリのPages設定（Source）が「Deploy from a branch」（レガシー方式）のままで、Actions側のワークフローと二重にデプロイを試みて競合していたことが直接の引き金と判明（`build_type: legacy`）。フリーズしていた古いデプロイ（`pages/deployments/{sha}/cancel`）を強制キャンセルして解消。Pages Sourceを「GitHub Actions」（`build_type: workflow`）に切り替えたが、切り替え後もタイムアウトが再発したため、レガシー方式に戻した。手動トリガーのビルド（`/pages/builds`）も失敗が続いたが、通常のgit push起点のビルド（コミット`d5c7ba4`）で正常に復旧した。
  - **現状**：Pages Sourceは「Deploy from a branch」（レガシー方式、元の状態）のまま。サイトは正常配信を確認済み（Last-Modified・署名内容とも最新コミットと一致）。
  - **未解決の構造的リスク**：`.github/workflows/deploy.yml`（GitHub Actions方式）とPages Source設定（レガシー方式）の不整合自体は解消していないため、理論上は今後も同様の競合・失敗が再発しうる。次に同様の障害が起きた場合は、本記録の調査手順（Pages API・deployments APIでの状態確認、フリーズしたデプロイのキャンセル、push起点ビルドでの復旧）を参照する。恒久対応（Pages Sourceを「GitHub Actions」に正式移行するか、`deploy.yml`を削除してレガシー方式に一本化するか）は次回、時間に余裕がある時に利用者と相談のうえ検討する。
- 納品メール下書きの署名テキストの行間修正（2026-08-28）の実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - Gmail下書き作成後、署名部分に「社会保険労務士齊藤事務所」の下・住所以下5行の下（キャッチコピーの上）にそれぞれ空行が入って見やすくなっていること
- 納品メール下書きの署名テキスト修正（2026-08-28）の実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - Gmail下書き作成後、本文末尾の署名が罫線・全角スペース行・各項目とも指定どおりに表示されていること（改行位置含む）
  - Gmail側の自動署名が有効な場合、二重表示になっていないか（既知の課題の別項目「署名の二重表示」と合わせて確認）
- 納品メール下書きポップアップの外側クリック誤閉じ修正（2026-08-28）の実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - ポップアップの外側（背景）をクリックしても閉じないこと
  - フォルダ名・説明書きを入力した状態で外側をクリックしても入力内容が消えないこと
  - ヘッダーの「✕」ボタンと下部の「閉じる」ボタンはこれまで通り押せば閉じること
- 納品メール下書き機能の3点修正（2026-08-28）の実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - ステータスを「納品済」に変更しても自動でポップアップが出ないこと、「📧」ボタンを押したときだけポップアップが開くこと
  - 「フォルダを追加」で入力欄が増え、「削除」で減らせること（1件のみのときは削除ボタンが出ないこと）
  - フォルダ名の下の説明書き欄が空欄でも保存・下書き作成に支障がないこと
  - 複数フォルダ入力時、下書き本文が「フォルダ名：〇〇」＋説明書き（あれば）の組を空行区切りで並べた形式になっていること（依頼文面の例と一致するか）
  - 本文末尾に署名（住所・TEL・FAX・Mail・Web・キャッチコピー）が自動で入っていること
  - **重要**：実際にGmailの下書き作成画面を開き、本文に含めた署名とGmail自身の自動署名が二重に表示されていないか確認する。二重になっている場合はGmail設定（歯車 → すべての設定を表示 → 全般 → 署名）で自動署名を無効化するか、下書きごとに手動で片方を削除する
- 納品メール下書きの宛名形式修正（2026-08-28）の実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - 担当者が複数人の場合、下書き本文・ポップアップ内プレビューの両方で「田中様」改行「浅野様」のように1人ずつ改行され「様」がつくこと
  - 担当者が1人の場合・未登録の場合は従来通り1行で表示されること（未登録時は「様」のみ）
- 納品メール下書き自動作成機能（2026-08-28）の実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - 案件管理でステータスを「納品済」に変更した瞬間にポップアップが表示されること
  - 「納品済」の行にのみ「📧」ボタンが常時表示され、クリックで同じポップアップを再表示できること（取消済みの行には表示されないこと）
  - ポップアップの会社名・宛先メールが顧問先マスタの値と一致すること、担当者メール未登録の顧問先では空欄になること
  - 「Gmailに下書きを作成」ボタンでGmailの作成画面が新規タブで開き、宛先・件名・本文（フォルダ名を含む）が自動入力されていること
  - 担当者名が複数人の場合の苗字抽出結果（「、」区切り・「・」区切りそれぞれ）が実データで想定通りになっているか（3文字以上の苗字は誤って2文字に切られる可能性があるため要確認）
  - ポップアップを閉じても既存の案件データ・ステータスに影響がないこと、他画面（請求集計・月変チェック・年更算定・給与計算・マスタ管理）に影響がないこと
- 業務監査員Ver.0の正本はGASエディタ上にあり、GitHub管理されていない
- Google Chat Webhookのスクリプトプロパティ移行は、稼働安定後に対応する保留課題
- 給与単価設定の前月自動引き継ぎ対応（2026-08-21）の実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - 当月未入力の顧問先で、給与計算固定金額・システム利用料に前月の値が自動セットされること
  - 当月に既に保存済みの値がある場合、上書きされず保存済みの値のまま表示されること
  - 追加案件・賞与計算固定金額は引き続き空欄（月ごとにリセット）のままであること
- 請求集計の「← 前月を一括引き継ぎ」ボタン（2026-08-21）の実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - ボタンクリックで確認ダイアログが表示されること
  - 実行後、前月データがある顧問先の単価が一括でセットされ、Firebase（`payrollRates`）に保存されること
  - 当月に既にデータ入力済みの顧問先は上書きされないこと
  - 追加案件・賞与計算固定金額は引き継がれず空欄のままであること
  - 実行後に件数バナーが表示され、しばらくすると消えること
  - 確定済みの月ではボタン自体が表示されないこと
- 一括引き継ぎの賞与請求額除外修正（2026-08-21）の実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - 賞与人数が入力済みの顧問先で一括引き継ぎ後も「賞与請求額」列が空欄（—）のままであること
  - 給与人数が入力済みの顧問先では、一括引き継ぎ後に「給与請求額」列が段階単価から通常通り計算されること（賞与のみ除外され、給与は影響を受けない）
  - その顧問先のモーダルを個別に開いて保存すると、以降は賞与請求額も段階単価から通常通り計算されるようになること
- 「案件請求」取消済み除外修正（2026-08-22）の実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - 株式会社カノエの請求集計（2026年8月）で「案件請求」列が0件・空欄になること（対象月が既に確定済みの場合は確定解除してから確認）
  - 通常の取消されていない請求済案件は、これまで通り正しく集計されること
  - 月変請求・年更算定請求の集計に変更がないこと

## 2026年9月6日 実施内容
【手続きマスタの強化】
- 手続きマスタにグループ設定を追加（入社・退職・身上異動・給与変更・賞与支給・出産・育児・介護・年齢到達・ケガ・病気・死亡・労働保険申告・算定基礎届・協定届・事業所に関する届出・その他）
- 手続きマスタにグループ絞り込みフィルターを追加
- 案件登録時の手続き選択がグループ別（optgroup）に表示されるように変更

【日付フィールドの追加】
- 手続きマスタに必須日付フィールドのチェックボックスを追加（取得日・喪失日・退職日・出産予定日・出産日・産前開始日・産後終了日・育児休業開始日・育児休業終了日）
- 案件登録・編集モーダルに日付入力欄を追加（手続きマスタで設定した必須フィールドのみ表示・カレンダー選択）（必須フィールドが未入力の場合は保存不可）
- 目的：給与計算システムとの将来的な連携

【給与計算との連携（イベント漏れ防止）】
- 月次データ入力ポップアップに関連案件を表示
  - 当月・翌月に日付フィールドが該当する案件を自動表示
  - 案件管理のメモ（被保険者名）も表示
  - 月変チェックの改定月・手続き対象者も表示
  - 手続きマスタのグループ別に折りたたみ表示（初期状態は折りたたみ、クリックで展開）
- 給与計算画面に年間全体イベントを常時横並び表示
  - 社会保険料率改定（3月）・雇用保険料率改定（4月）・住民税更新（6月）・算定基礎届適用（9月）・最低賃金改定（10月）・年末調整還付（12月・1月）
  - 当月・翌月は青枠・太字で強調表示、それ以外はグレーで薄く表示

## 2026年8月27日 実施内容
- 請求集計画面に「← 前月を一括引き継ぎ」ボタンを追加
  - 対象項目：労務顧問・給与計算（段階単価・固定金額）・処遇改善加算・システム利用料
  - 除外項目：追加案件・賞与計算
  - 確認ダイアログあり
- 請求集計の案件請求集計に`!c.cancelled`条件を追加（バグ修正）
  - 取消済み案件が請求集計に混入する問題を解消
  - 同様の問題が藤野興業（2026年7月）にも存在し同時解消

## 2026年8月3日 実施内容
- 年更・算定画面の「対象年度」が改行される問題を修正（index.html 2626行目のtdにwhitespace-nowrapを追加）
- 36協定通知の改善
  - 起算日を過ぎた顧問先を通知から非表示
  - 手続き済・納品済の場合、起算日の横にステータスバッジを表示
- 月変チェック新規追加モーダルを編集モーダルと項目統一
  - 追加項目：No・チェック対象者・手続き対象者・改定人数・ステータス・請求額
  - ステータス初期値：依頼受付
  - 請求額はisHiro制御（hiro@saitoh-sr.comのみ表示）
- データ一括出力機能を追加
  - ボタンラベル：📥 データ一括出力
  - 出力形式：Excel（.xlsx）
  - シート構成：案件管理・月変チェック・年更算定・給与計算・顧問先マスタ
  - 案件管理は請求済・取消済を除外
  - 給与計算は全年月フラット化で出力
  - 請求金額は含めない
  - 全アカウントに表示

## NEXT_ACTION
- 次回作業開始時に `git fetch origin`、指示文書、Git状態を再確認する
- 次回ログイン時（できればシステム日付が3月・4月・9月・10月付近のタイミング）に、給与計算画面の年間全体イベント表示（当月徴収・翌月徴収の分割・強調デザイン、2026-09-06）を実ブラウザで確認する（上記「既知の課題」参照）
- 【残課題】育児関係で1名に対して複数手続きが表示される問題への対応方針を利用者と検討する（上記「既知の課題」参照）
- 【残課題】業務監査員Ver.0への給与計算イベント通知の追加（Step2）を対応する（上記「既知の課題」参照）
- 次回ログイン時に、給与計算「月次データ入力」ポップアップの関連案件グループ別折りたたみ表示（2026-09-06）を実ブラウザで確認する（上記「既知の課題」参照）
- 次回ログイン時に、請求集計「CSVダウンロード」からシステム利用料（内訳コード27）が除外されていること・画面表示は従来通りであることを実ブラウザで確認する（上記「既知の課題」参照）
- 【最優先】納品メール下書きの「様」自動付与を削除したため（2026-08-28）、既存の顧問先マスタ「宛名」欄に「様」を含まない値が登録されている場合、下書きの宛名も「様」なしで表示される。利用者に、必要な顧問先の宛名欄を「〇〇様」の形に更新してもらう必要があるかを確認・案内する
- 【最優先】次回ログイン時に、顧問先マスタの担当者②廃止・CCメール移行（2026-08-28）が実データで正しく行われたか必ず確認する（上記「既知の課題」参照）。マスタ管理画面を開くと自動でFirebaseデータが書き換わるため、確認前に不用意に他の変更を加えない
- 次回ログイン時に、給与単価設定の前月自動引き継ぎ対応・一括引き継ぎボタン・賞与請求額除外修正（2026-08-21）・案件請求の取消済み除外修正（2026-08-22）を実ブラウザで確認する（上記「既知の課題」参照）
- 株式会社カノエの請求集計（2026年8月）画面で、対象月が「確定済み」になっていないか確認する（確定済みの場合は「確定解除」しないと今回の修正が画面に反映されない）

## 最終更新
- 最終更新AI: Claude Code
- 最終更新日時: 2026-09-06（日本時間）
- 変更内容: 給与計算「月次データ入力」ポップアップの月変チェック（改定月）を「月額変更届」グループとして折りたたみ表示に追加。表示ループを`[...PROCEDURE_GROUPS, '月額変更届'].map(...)`に変更し、末尾の専用グループで`relatedChecks`を表示（`isCheckGroup`フラグで分岐、他グループと同じ見出しクリック開閉に統一）。手続きマスタのグループ「給与変更・賞与支給」とは別枠の専用グループ名として扱う（`PROCEDURE_GROUPS`定数自体は変更なし）。子要素の表示内容（「月額変更届｜改定月：〇〇｜手続き対象者」）は変更前と同一。`relatedCases`・`relatedChecks`のデータ取得ロジック・`groupedCases`の判定・データ構造・他画面には変更なし。Babelで構文検証済み。実ブラウザでの折りたたみ・展開動作の確認は未実施（Chrome拡張機能未接続のため）。

### 過去の更新
- 2026-09-06（Claude Code）: 給与計算画面の年間全体イベント表示をバッジ風デザインに改善。当月・翌月（`state:'current'`または`'next'`）を、従来のオレンジ塗りつぶし（当月：オレンジ背景・白文字／翌月：薄いオレンジ背景・オレンジ文字）から、白背景・オレンジ枠線（`1.5px solid #ea580c`）・オレンジ太字の塗りつぶしなしアウトライン表示に統一（当月・翌月の見た目の区別はなくし、どちらも同じ控えめな強調に変更）。それ以外（`state:'none'`）は薄いグレー背景（`#f3f4f6`）・グレー枠線（`#d1d5db`）・グレー文字（`#6b7280`）の通常バッジに変更（角丸`borderRadius:'9999px'`で全バッジ共通のバッジ形状に統一）。当月徴収・翌月徴収の分割表示ロジック・強調判定ロジック・ラベル文言・他画面・データ構造には変更なし。Babelで構文検証済み、Node.jsで9月・3月表示時のハイライト対象が依頼の表示イメージと一致することを確認済み。実ブラウザでの表示確認は未実施（Chrome拡張機能未接続のため）。最終更新日時: 2026-09-06（日本時間）
- 2026-09-06（Claude Code）: 給与計算画面の年間全体イベント強調表示の色を青系からオレンジ系に変更。右上の「年間一覧」「顧問先追加」ボタン（青系）と色が被り目立たなかったための修正。当月（`state:'current'`）を青背景・白文字（`#2563eb`）からオレンジ背景・白文字（`#ea580c`）に、翌月（`state:'next'`）を薄い青背景・青文字（`background:'#dbeafe'`,`color:'#1d4ed8'`）から薄いオレンジ背景・オレンジ文字（`background:'#ffedd5'`,`color:'#c2410c'`）に変更。非該当（グレー文字）・当月徴収/翌月徴収の分割表示ロジック・強調判定ロジック・他画面・データ構造には変更なし。Babelで構文検証済み。実ブラウザでの表示色確認は未実施（Chrome拡張機能未接続のため）。最終更新日時: 2026-09-06（日本時間）
- 2026-09-06（Claude Code）: 給与計算画面の年間全体イベント表示を修正。①「社会保険料率改定」（3月）・「算定基礎届適用」（9月）を当月徴収・翌月徴収の2件に分割表示（`PAYROLL_EVENTS`に`split:true`フラグ追加、`getPayrollEventDisplayItems`新規関数で分解）。②強調表示デザインを、当月は青背景・白文字、翌月は薄い青背景・青文字、非該当はグレー文字（現状維持）に変更（`isPayrollEventActive`を`getPayrollEventState`に置き換え、`'current'`/`'next'`/`'none'`の3状態で判定）。分割対象外の4イベント・他画面・データ構造には変更なし。Babelで構文検証済み、Node.jsで3月・4月・9月・10月等7パターンの強調表示ロジックをシミュレーションし依頼仕様との一致を確認済み。実ブラウザでの表示・強調色の確認は未実施（Chrome拡張機能未接続のため）。最終更新日時: 2026-09-06（日本時間）
- 2026-09-06（Claude Code）: ドキュメントのみの更新（コード変更なし）。2026年9月6日に実施した手続きマスタの強化（グループ設定・絞り込みフィルター・グループ別optgroup表示）、日付フィールドの追加（必須日付フィールドのチェックボックス・案件登録編集モーダルの日付入力欄・未入力時保存不可）、給与計算との連携（月次データ入力ポップアップの関連案件表示・グループ別折りたたみ表示、給与計算画面の年間全体イベント常時横並び表示）を「2026年9月6日 実施内容」セクションとしてPROJECT_STATUS.mdに追記。あわせて残課題（育児関係で1名に対して複数手続きが表示される問題、業務監査員Ver.0への給与計算イベント通知追加）を「既知の課題」「NEXT_ACTION」に記録。最終更新日時: 2026-09-06（日本時間）
- 2026-09-06（Claude Code）: 給与計算「月次データ入力」ポップアップの関連案件表示を、手続きマスタのグループ設定に基づくグループ別折りたたみ表示に変更。`PayrollMonthModal`に`masters`propを追加し、`groupedCases`（`useMemo`）で手続き名から`masters.procedures`のグループを判定、`PROCEDURE_GROUPS`順に「▶/▼ グループ名 件数」の見出し（クリックで開閉、`expandedGroups` state）と、展開時の子要素（手続き名｜日付｜メモ）を表示。グループ未設定・マスタ未登録の手続きは「その他」に分類。月変チェック表示・データ構造・案件管理画面には変更なし。Babelで構文検証済み、実ブラウザでの動作確認は未実施（Chrome拡張機能未接続のため）。最終更新日時: 2026-09-06（日本時間）
- 2026-09-03（Claude Code）: 請求集計「CSVダウンロード」（かつ・かいしゅう用）からシステム利用料（内訳コード27）の行を除外。来月からポータルの請求業務ウィザードSTEP1でシステム利用料を自動計算しSTEP3のCSVに含める運用に変更するため、二重出力を避ける目的。`index.html`の`handleDownloadCSV`内で内訳コード'27'を追加する行を削除。請求集計画面のシステム利用料入力欄・表の列表示、既存の`billingSnapshots`等のデータには影響なし。実ブラウザでの動作確認は未実施（Chrome拡張機能未接続のため）。

- 2026-08-28（Claude Code）: 顧問先マスタの「担当者名」表記を「宛名」に変更（編集フォーム・詳細表示・一覧テーブル、顧問先タブのみ。データの内部キーは`担当者名`のまま、案件管理の担当者・提携先マスタ・shogu-app連携は対象外）。あわせて納品メール下書きの宛名から「様」自動付与処理を削除し、マスタの宛名欄に登録された値をそのまま使用する方式に変更（プレースホルダーの例も「田中様、浅野様」に更新）。**既存の宛名データに「様」を含まない値がある場合、下書きの宛名も「様」なしで表示される点に注意**（詳細は「既知の課題」「NEXT_ACTION」参照）。実ブラウザでの動作確認は未実施（Chrome拡張機能未接続のため）。最終更新日時: 2026-08-28（日本時間）

- 2026-08-28（Claude Code）: 顧問先マスタ編集フォームの担当者名プレースホルダーを「苗字のみ。複数人は「、」区切り（例：田中、浅野）」から「メールに表示する名前。複数は「、」区切り（例：田中、浅野）」に修正。担当者メール（To）・CCメールのプレースホルダーは変更なし。低リスクな文言修正のため実ブラウザでの動作確認は省略。最終更新日時: 2026-08-28（日本時間）
- 2026-08-28（Claude Code）: 納品メール下書きの宛名を、担当者名から苗字を自動抽出する方式（先頭2文字を切り詰め）から、顧問先マスタに登録された担当者名をそのまま使用する方式に変更。`extractSurnames`関数を廃止し`splitNames`関数に置き換え。「、」区切りの場合は改行して各自に「様」をつける（例：「田中、浅野」→「田中様」改行「浅野様」）。切り詰め処理は完全に削除したため、英語名や3文字以上の苗字でもそのまま表示される。実ブラウザでの動作確認は未実施（Chrome拡張機能未接続のため）。最終更新日時: 2026-08-28（日本時間）
- 2026-08-28（Claude Code）: 【データ構造変更・利用者承認済み】顧問先マスタの担当者②（担当者名②・担当者メール②）を廃止し、CCメール欄を新設。既存データはアプリ起動時に自動移行（担当者メール②→CCメール、担当者名②→担当者名に「、」結合）し、Firebase本番データも書き換わる（`MASTER_VERSION` 3→4）。納品メール下書き機能のCCにも反映。実ブラウザでの動作確認・実データでの移行結果確認は未実施（Chrome拡張機能未接続のため）。最終更新日時: 2026-08-28（日本時間）
- 2026-08-28（Claude Code）: task-app内の全ポップアップ・モーダル（`modal-overlay`を使う16箇所全て）について、背景（外側）クリックで閉じてしまう挙動を削除。以後は各モーダルの「✕」または「キャンセル／閉じる」ボタンを押したときのみ閉じる。入力途中のフォームデータが誤操作で消えるのを防止。実ブラウザでの動作確認は未実施（Chrome拡張機能未接続のため）。最終更新日時: 2026-08-28（日本時間）
- 2026-08-28（Claude Code）: 納品メール下書き機能を2点修正。①フォルダ名入力欄のプレースホルダー例示テキストを削除。②担当者名の区切り文字を「、」のみに変更し、「・」「,」「，」は区切りとして扱わず氏名の一部とみなすよう`extractSurnames`を修正。あわせて、直前に発生したGitHub Pagesデプロイ障害（約45分間）の調査経緯・復旧内容を記録（解決済み）。実ブラウザでの動作確認は未実施（Chrome拡張機能未接続のため）。最終更新日時: 2026-08-28（日本時間）
- 2026-08-28（Claude Code）: 納品メール下書きの署名テキストの行間を修正。罫線の下・「社会保険労務士齊藤事務所」の下・住所以下5行の下（キャッチコピーの上）にそれぞれ空行を挟む形式に`DELIVERY_MAIL_SIGNATURE`定数を差し替え。文言・罫線記号・チルダ等の文字自体は変更なし。実ブラウザでの動作確認は未実施（Chrome拡張機能未接続のため）。最終更新日時: 2026-08-28（日本時間）
- 2026-08-28（Claude Code）: 納品メール下書きの署名テキストを指定書式に修正。`＿/`の罫線で上下を囲み、罫線直下に全角スペース行を挟んで「社会保険労務士齊藤事務所／住所／TEL／FAX／Mail／Web／キャッチコピー」を記載する形式に`DELIVERY_MAIL_SIGNATURE`定数を全面差し替え。実ブラウザでの動作確認は未実施（Chrome拡張機能未接続のため）。最終更新日時: 2026-08-28（日本時間）
- 2026-08-28（Claude Code）: 納品メール下書きポップアップの外側クリックによる誤閉じを修正。背景クリックで`onClose`が呼ばれる処理を削除し、ヘッダーの「✕」ボタンと下部の「閉じる」ボタンを押したときのみ閉じるよう変更（入力中のフォルダ名・説明書きが誤って消えないようにするため）。実ブラウザでの動作確認は未実施（Chrome拡張機能未接続のため）。最終更新日時: 2026-08-28（日本時間）
- 2026-08-28（Claude Code）: 納品メール下書き機能を3点修正。①ステータスを「納品済」に変更した瞬間の自動ポップアップ表示を削除し、「📧」ボタンを押したときのみ表示するよう変更。②フォルダ名を複数行入力できるよう変更（「フォルダを追加」「削除」ボタン、各フォルダに任意の説明書き欄）。③本文末尾に事務所の固定署名を自動追加。Gmail側の自動署名との二重表示リスクについては次回ログイン時の実機確認が必要。実ブラウザでの動作確認は未実施（Chrome拡張機能未接続のため）。最終更新日時: 2026-08-28（日本時間）
- 2026-08-28（Claude Code）: 納品メール下書きの宛名形式を修正。複数の担当者がいる場合「田中・浅野様」と1行にまとめていたのを、担当者ごとに改行し「田中様」「浅野様」のようにそれぞれ「様」をつける形式に変更（下書き本文・ポップアップ内プレビューの両方）。実ブラウザでの動作確認は未実施（Chrome拡張機能未接続のため）。最終更新日時: 2026-08-28（日本時間）
- 2026-08-28（Claude Code）: 案件管理（手続き一覧）にGmail納品メール下書き自動作成機能を追加。ステータスを「納品済」に変更した瞬間にポップアップを表示し、フォルダ名を入力して「Gmailに下書きを作成」を押すと、顧問先マスタから会社名・担当者メールを自動取得したGmail作成画面が新規タブで開く。「納品済」行には📧ボタンを常時表示し、再表示可能。実ブラウザでの動作確認は未実施（Chrome拡張機能未接続のため）。最終更新日時: 2026-08-28（日本時間）
- 2026-08-22（Claude Code）: 請求集計「案件請求」列の集計フィルターに取消済み案件の除外（`!c.cancelled`）が漏れていた不具合を修正。月変請求・年更算定請求と同じ条件に統一した。実ブラウザでの動作確認は利用者の判断により未実施。最終更新日時: 2026-08-22（日本時間）
